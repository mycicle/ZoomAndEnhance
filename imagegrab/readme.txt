run doit.m
Change the input parameter of frame_extrantor to your video file's name(without .mp4).
The other parameter is the frame grab rate.

Example:
frame_extractor('test', 10);

This reads the video file test.mp4 and grabs every 10th frame starting at 1.

